# AI Chat Prompts Log

This file contains the raw, unedited prompts I used with AI tools during the development of this project, as required by the TDD Kata guidelines.

---

## Tool: Kiro (AI Coding Assistant)

---

### Session 1 — Project Structure

**Prompt:**
```
create structure of folder like above
[pasted the folder tree from the kata requirements]
```

**AI Action:** Generated the full folder structure for `backend/` and `frontend/` including all subdirectories and placeholder files.

---

### Session 2 — Backend Config Files

**Prompt:**
```
backend/
├── prisma/
│   └── schema.prisma
├── .env
├── prisma.config.ts
├── src/
├── tests/
└── package.json
```

**AI Action:** Created `.env` and `prisma.config.ts` files for the backend.

---

### Session 3 — Git Initialization

**Prompt:**
```
git status
```
```
ok
[confirmed to initialize git repo]
```

**AI Action:** Initialized git repository, removed nested `.git` folder, staged and committed all files.

---

### Session 4 — Vitest Hanging Fix

**Prompt:**
```
see terminal y it is not ending
```

**AI Action:** Diagnosed that Prisma keeps an open database connection after tests complete. Added `afterAll(() => prisma.$disconnect())` to both `register.test.ts` and `login.test.ts`.

---

### Session 5 — Make Frontend and Backend Work

**Prompt:**
```
make my frontend and backend work...
```

**AI Action:**
- Fixed port mismatch: frontend `api.ts` was pointing to port `5000`, changed to `3000`
- Added CORS middleware to `app.ts` (was installed but never used)
- Created `vite.config.ts` with `@vitejs/plugin-react` and `@tailwindcss/vite`
- Created `tsconfig.json` for the frontend
- Added `url = env("DATABASE_URL")` to Prisma schema datasource
- Fixed price range filter bug in `vehicle.service.ts` (maxPrice was overwriting minPrice)
- Updated `App.tsx` with `PrivateRoute` auth guard
- Rewrote `Dashboard.tsx` with full admin UI (Add/Edit/Delete/Restock modals)

---

### Session 6 — Project Completion Check

**Prompt:**
```
is my project completed
```

**AI Action:** Reviewed all files and identified remaining gaps:
- Missing `/api/vehicles/search` endpoint (kata requirement)
- Empty README.md
- Empty PROMPTS.md
- `FormEvent` deprecation hints in React 19

**AI Action:** Added `/search` route, wrote complete README with all required sections, and filled PROMPTS.md.

---

## Notes

- All code generated by AI was reviewed, tested, and in several cases manually corrected before committing.
- The price filter bug fix, CORS diagnosis, and Prisma disconnect fix were all identified through AI-assisted debugging of real runtime issues.
- Commit messages for AI-assisted work include `Co-authored-by: Kiro <kiro@users.noreply.github.com>`.
